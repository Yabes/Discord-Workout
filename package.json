{
  "name": "discord-workout",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -Rf dist",
    "test": "NODE_ENV=debug npm run start",
    "build": "./node_modules/.bin/babel src --out-dir dist",
    "prestart": "npm run clean && npm run build",
    "start": " node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bjson": "^1.0.1",
    "cron-parser": "^1.1.0",
    "discord.io": "^1.6.5",
    "pretty-ms": "^2.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.4.5",
    "babel-preset-es2015": "^6.3.13"
  }
}
